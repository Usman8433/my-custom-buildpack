#!/bin/sh

echo "-----> Custom Buildpack: Running pip install --no-deps -r requirements.txt"

# Install the pip requirements without their dependencies
pip install --no-deps -r "$BUILD_DIR/requirements.txt"

