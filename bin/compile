#!/bin/sh

echo "-----> Custom Buildpack: Installing Python and packages"

# Install Python and required packages
apt-get update
apt-get install -y python3 python3-pip

# Install Python packages from requirements.txt
pip3 install --no-deps -r "$BUILD_DIR/requirements.txt"